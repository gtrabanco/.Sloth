#!/usr/bin/env bash

os=$(uname)
if [[ "$os" == "Linux" ]]; then
  if command -v xclip &> /dev/null; then
    xclip -selection clipboard -o
  else
    echo "No xclip found"
    exit 1
  fi
elif [[ "$os" == "Darwin" ]]; then
  /usr/bin/pbpaste
else
  echo "Unsupported OS"
fi
